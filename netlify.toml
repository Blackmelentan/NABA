[build]
  publish = "site"
  command = ""

[context.production.environment]
# Set Netlify environment variables in the Site UI (do not commit secrets here).
# SUPABASE_URL = "https://<project>.supabase.co"
# SUPABASE_ANON_KEY = "<anon-key>"
